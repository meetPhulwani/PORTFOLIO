/**
 * Layout breakpoints aligned with Tailwind defaults (px).
 * Prefer Tailwind responsive classes in UI; use these for JS media queries.
 */
export const BREAKPOINTS = {
  sm: 640,
  md: 768,
  lg: 1024,
  xl: 1280,
  '2xl': 1536,
} as const
